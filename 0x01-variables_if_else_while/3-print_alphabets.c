#include <stdio.h>
int main(void)
{
	char a;
	char z;
	char alphabet;
	
	while (a = 'a'; a <= 'z'; a++)
		a = alphabet;
	for (z = 'A'; z <= 'Z'; z++)
		z = alphabet;
			putchar(alphabet);
		putchar('\n');
	return (0);
}
