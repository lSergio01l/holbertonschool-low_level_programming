#include <stdio.h>
/**
* main - print the numbers
* Return: 0
*/
int main(void)
{
	int numbers;

	for (numbers = 0; numbers < 10; numbers++)
		printf("%numbers", numbers);
	printf('\n');
	return (0);
}
